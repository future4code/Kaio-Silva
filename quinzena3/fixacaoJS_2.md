```js
function calculaPrecoTotal(quantidade) {
 if(quantidade < 12  ){
  const  totalCompra = quantidade * 1.3
  return totalCompra
    
  } else {
  const  totalCompra = quantidade * 1
    return totalCompra
  }
}
```
